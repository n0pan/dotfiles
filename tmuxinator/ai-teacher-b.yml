# /Users/lq/.config/tmuxinator/ai-teacher-b.yml

name: ai-teacher-b
root: ~/dev/woodside/ai-teacher-backend
pre_window: source ~/.zshrc && source venv/bin/activate
windows:
  - server:
      layout: even-horizontal
      panes:
        - api-server: cd src && uvicorn api:app --host 0.0.0.0 --port 8000 --reload
        - agent-server: python src/agent.py dev
  - editor: nvim
